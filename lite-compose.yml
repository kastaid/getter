version: "3.9"
services:
  getter:
    build:
      context: .
      dockerfile: lite.Dockerfile
    image: getter:latest
    volumes:
      - ./:/app
    env_file:
      - config.env
    restart: no
    network_mode: bridge
