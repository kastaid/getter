version: "3.9"
services:
  getter:
    build:
      context: .
      dockerfile: lite.Dockerfile
    image: getter:latest
    volumes:
      - ./:/app
    env_file:
      - config.env
    restart: unless-stopped
    network_mode: bridge
    deploy:
      resources:
        limits:
          memory: 128M
