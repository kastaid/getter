version: "3.9"
services:
  getter:
    build: .
    dockerfile: lite.Dockerfile
    image: getter:latest
    volumes:
      - ./:/app
    env_file:
      - config.env
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1024M
