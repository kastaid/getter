version: "3.9"
services:
  getter:
    build: .
    image: getter:latest
    container_name: getter
    volumes:
      - ./:/home/app/getter
    env_file:
      - config.env
    restart: always
    deploy:
      resources:
        limits:
          memory: 1024M
networks:
  default:
    name: getter
